<h1>profile</h1>
    
    <h2>hola {{userData.first_name}} {{userData.last_name}}</h2>
    <h3>te registraste con el correo {{userData.email}}</h3>
    <h4>tu rol en la app es {{userData.role}}</h4>
    
    {{#if userData.isAdmin}}
{{else}}
    {{#if isPremium}}
    <h2>la url para tu tienda es http://localhost:8080/myshop/{{userData._id}}</h2>
    <a href="/adddesign">addDesign</a>
    <button onclick="becomePremium()">conviertete en user aqui</button>
    {{else}}
    <h2>si quieres subir diseños debes ser usuario premium</h2>
    <button onclick="becomePremium()">conviertete en premium aqui</button>
    {{/if}}
{{/if}}
    {{#if userData.isAdmin}}
        <h1>eres admin y puedes ver esto</h1>
        <a href="/usersManager">UserManager</a>
        <a href="/deleteDes">deleteDesign</a>
        <a href="/roleupdate">role update</a>
        
    {{/if}}
    
    <script src="./js/profile.js"></script>